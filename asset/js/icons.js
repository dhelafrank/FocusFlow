export function setIcon(element, iconName) {
    let icon = `/Asset/icons/${iconName}.svg`
    element.innerHTML = `<img width="70%" height="" src=${icon}>`
    // console.log(element);
}
export function setMiniIcon(element, iconName) {
    let icon = `/Asset/icons/${iconName}.svg`
    element.innerHTML = `<img width="70%" height="" src=${icon}>`
    // console.log(element);
}